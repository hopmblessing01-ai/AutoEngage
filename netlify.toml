[build]
  command = "npm run build"

# .env.example is documentation only (no real keys).
[build.environment]
  SECRETS_SCAN_OMIT_PATHS = ".env.example"

[[plugins]]
  package = "@netlify/plugin-nextjs"
